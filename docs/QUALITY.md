# v1.0 本地质量报告

检查日期：2026-08-14

## 已通过

- `npm.cmd run check`：格式、ESLint、严格 TypeScript、生产构建全部通过；
- Vitest：6 个测试文件、22 项测试全部通过；
- 核心领域模块覆盖率：语句 100%、函数 100%、行 100%、分支 98.3%；
- Playwright：27 项端到端测试全部通过，覆盖 390×844 手机 Chrome、1440×900 桌面 Chrome 和 Edge；
- axe：完整颜色对比规则开启，无严重或关键无障碍问题；
- PWA：生产 Service Worker 生成成功，应用壳可离线重载；
- Lighthouse 移动端：性能 98、无障碍 100、最佳实践 96、CLS 0。

端到端流程包括首次引导、科研模板、实时计时、暂停与完成、手动补录、刷新恢复、编辑与软删除、JSON 导出/恢复、计划与复盘、核心页面、离线重载和响应式截图。

## 发布前仍需外部验收

- 创建真实 Supabase 项目并执行迁移；
- 验证邮箱验证码、两设备增量同步、离线恢复、同时编辑冲突、活动计时器冲突、删除传播和 RLS 用户隔离；
- 在真实 Android Chrome 上完成安装、安全区、键盘、后台切换和网络切换检查；
- 配置 GitHub Actions 仓库变量并验证 Pages 正式地址。

上述项目依赖用户自己的 Supabase 与 Android 设备，因此不以本地演示配置伪造通过结果。
