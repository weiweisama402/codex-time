# 时衡 v2.0 候选版质量报告

检查日期：2026-08-14

## 已验证

- 严格 TypeScript 生产构建通过，PWA Service Worker 与离线应用壳生成成功；
- ESLint 与 Prettier 零错误；
- Vitest：8 个测试文件、23 项测试通过；核心 `src/lib` 覆盖率为行 100%、函数 100%、语句 99.22%、分支 92.64%；
- 单元/组件范围覆盖计时恢复、暂停累计、跨午夜与闰年/时区、日周月统计、软删除清理、原子导入、云修订策略、计时表单、迷你计时器、补录、邮箱密码入口和冲突选择；
- Playwright：Chrome 360×800 与 Edge 412×915 共 20 项全部通过，覆盖首次使用、刷新恢复、暂停/继续/停止、迷你计时、补录、编辑/复制/删除/撤销、月统计、JSON 导入导出、Android 返回键、离线重载、截图和 axe；
- Lighthouse 移动审计：性能 97、无障碍 100、最佳实践 100、CLS 0；
- Supabase `mobile_backups` 迁移已应用：强制 RLS 开启，`anon` 无表权限，`authenticated` 仅有 select/insert/update，三条本人策略存在；
- 回滚集成探针验证本人可见、其他用户不可见、正确修订更新成功、旧修订更新返回 0 行，未留下测试备份。

## 顾问结果

- 数据表没有 RLS 安全告警；Supabase 安全顾问仅提示项目级“泄露密码保护未开启”，建议在 Auth 密码设置中开启；
- 性能顾问仅提示 v1 `entities_user_type_idx` 尚未使用；v2 不读取该表，因此不删除 v1 索引。

## 发布前外部验收

- 等待 GitHub Actions 在候选分支再次执行相同检查；
- 按 `ANDROID_QA.md` 在真实 Android Chrome/Edge 验收安装、软键盘、安全区、后台计时和网络切换；
- 用个人账号密码完成真实浏览器备份与换机恢复。自动化不会读取或记录密码。
